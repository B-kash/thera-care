{
  "name": "thera-care",
  "private": true,
  "description": "Physiotherapist practice app — root scripts for local dev",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:db": "docker compose up -d --no-recreate",
    "dev:multiplex": "concurrently -k -n web,api -c magenta,blue \"npm run dev --prefix frontend\" \"npm run start:dev --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
