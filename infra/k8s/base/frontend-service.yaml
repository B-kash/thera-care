apiVersion: v1
kind: Service
metadata:
  name: thera-care-frontend
  labels:
    app.kubernetes.io/name: thera-care-frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: thera-care-frontend
